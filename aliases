# vim: set ts=4 sw=4 et ft=sh:

# Unix
alias ll="ls -al"
alias mkdir="mkdir -p"

# Pretty print path
alias path='echo $PATH | tr -s ":" "\n"'

# Include custom aliases
if [[ -f ~/.aliases.local ]]
then
    source ~/.aliases.local
fi
